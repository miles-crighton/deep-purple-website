{
    "name": "website",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "dev": "concurrently \"nodemon build.js\" \"serve dist\"",
        "build": "node build.js",
        "deploy": "yarn build && gh-pages -d dist"
    },
    "dependencies": {
        "pug": "^3.0.2"
    },
    "devDependencies": {
        "concurrently": "^6.0.0",
        "copy": "^0.3.2",
        "del": "^6.0.0",
        "gh-pages": "^3.1.0",
        "glob": "^7.1.6",
        "gulp": "^4.0.2",
        "nodemon": "^2.0.7",
        "prettier": "^2.2.1"
    }
}
